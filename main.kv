#:import random random
#:import MOVEMENT_SPEED __main__.MOVEMENT_SPEED

<SnakePart>:
    size: MOVEMENT_SPEED, MOVEMENT_SPEED
    canvas.before:
        Color:
            rgb: random.random(), random.random(), random.random()
        Rectangle:
            size: self.size
            pos: self.pos

GameScreen:
    canvas.before:
        Color:
            rgb: 1, 1, 1
        Rectangle:
            size: self.size
            pos: self.pos
    Widget:
        size: MOVEMENT_SPEED, MOVEMENT_SPEED
        pos: 120, 120
        id: food
        canvas.before:
            Color:
                rgb: random.random(), random.random(), random.random()
            Rectangle:
                size: self.size
                pos: self.pos 

